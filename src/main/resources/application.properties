# Default Configuration (Development)
# This file contains default values. For production, use application-prod.properties
# For development, use application-dev.properties

# Database Configuration - Use environment variables with defaults
# These will be overridden by profile-specific properties
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.driver-class-name=${DATABASE_DRIVER}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.dialect=${HIBERNATE_DIALECT}
spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL:true}

# Email Configuration - Use environment variables with defaults
spring.mail.host=${MAIL_HOST:smtp-relay.brevo.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:8db010002@smtp-brevo.com}
spring.mail.password=${MAIL_PASSWORD:0nrsm7pxRv5f3zCU}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Thymeleaf Configuration
spring.thymeleaf.cache=${THYMELEAF_CACHE:false}
spring.thymeleaf.mode=HTML

# Server Configuration
server.port=${PORT:8080}

# Logging Configuration
logging.level.com.shteotkacha=${LOG_LEVEL:DEBUG}
logging.level.org.springframework.security=${SECURITY_LOG_LEVEL:DEBUG}

# Application Configuration
app.email.from=${APP_EMAIL_FROM:anikejov003@gmail.com}
app.email.verification-url=${APP_URL:http://localhost:8080}/verify

# Default Profile
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Actuator Configuration
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always 